apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: search-api-ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: PathPrefix(`/api/search`)
      middlewares:
        - name: strip-api
      services:
        - name: search-api
          port: 8000
