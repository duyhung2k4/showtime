apiVersion: v1
kind: Service
metadata:
  name: {{ include "ticket-api.fullname" . }}
  labels:
    {{- include "ticket-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ticket-api.selectorLabels" . | nindent 4 }}
