apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: ticket-api-ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: PathPrefix(`/api/tickets`)
      middlewares:
        - name: strip-api
      services:
        - name: ticket-api
          port: 8000
