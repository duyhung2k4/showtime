apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: user-api-ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: PathPrefix(`/api/users`)
      middlewares:
        - name: strip-api
      services:
        - name: user-api
          port: 8000
