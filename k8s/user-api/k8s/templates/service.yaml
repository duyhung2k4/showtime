apiVersion: v1
kind: Service
metadata:
  name: user-api
spec:
  selector:
    app: user-api
  type: ClusterIP
  ports:
    - name: user-api
      protocol: TCP
      port: 8000
      targetPort: 8000
