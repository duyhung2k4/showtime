apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: movie-api-ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: PathPrefix(`/api/movies`)
      middlewares:
        - name: strip-api
      services:
        - name: movie-api
          port: 8000
