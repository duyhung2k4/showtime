apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: auth-api-ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: PathPrefix(`/api/authentication`)
      middlewares:
        - name: strip-api
      services:
        - name: auth-api
          port: 8000
