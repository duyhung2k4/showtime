apiVersion: apps/v1
kind: Deployment
metadata:
  name: statistic-api
  labels:
    app: statistic-api
spec:
  selector:
    matchLabels:
      app: statistic-api
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: statistic-api
    spec:
      containers:
        - name: statistic-api-ctn
          image: nguyen040904/statistic-api:latest
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 100m
              memory: 100Mi
          ports:
            - containerPort: 8000
              name: sst-api-port
          volumeMounts:
            - name: env
              mountPath: /app/.env
              subPath: .env
      volumes:
        - name: env
          configMap:
            name: global-env
      restartPolicy: Always
