apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: seattype-api-ingress
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: PathPrefix(`/api/seattypes`)
      middlewares:
        - name: strip-api
      services:
        - name: seattype-api
          port: 8000
