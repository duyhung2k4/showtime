apiVersion: apps/v1
kind: Deployment
metadata:
  name: seattype-api
  labels:
    app: seattype-api
spec:
  selector:
    matchLabels:
      app: seattype-api
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: seattype-api
    spec:
      containers:
        - name: seattype-api-ctn
          image: nguyen040904/seattype-api:latest
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 100m
              memory: 100Mi
          ports:
            - containerPort: 8000
              name: seat-api-port
          volumeMounts:
            - name: env
              mountPath: /app/.env
              subPath: .env
      volumes:
        - name: env
          configMap:
            name: global-env
      restartPolicy: Always
