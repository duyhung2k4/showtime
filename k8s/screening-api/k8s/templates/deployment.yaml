apiVersion: apps/v1
kind: Deployment
metadata:
  name: screening-api
  labels:
    app: screening-api
spec:
  selector:
    matchLabels:
      app: screening-api
  replicas: 1
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: screening-api
    spec:
      containers:
        - name: screening-api-ctn
          image: nguyen040904/screening-api:latest
          resources:
            requests:
              cpu: 100m
              memory: 100Mi
            limits:
              cpu: 100m
              memory: 100Mi
          ports:
            - containerPort: 8000
              name: screen-api-port
          volumeMounts:
            - name: env
              mountPath: /app/.env
              subPath: .env
      volumes:
        - name: env
          configMap:
            name: global-env
      restartPolicy: Always
